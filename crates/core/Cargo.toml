[package]
name = "incidence-core"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
sha2 = "0.10"
thiserror = "2"
tracing = "0.1"

[dev-dependencies]
serde_json = "1"

[lints]
workspace = true
